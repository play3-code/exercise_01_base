let geodata = [];

let bounds = {
  left: 8.20782,
  top: 47.094669,
  right: 8.365691,
  bottom: 47.024504,
};

function setup() {
  createCanvas(900, 650);

  noLoop();
}

function draw() {
  background(250);
}
